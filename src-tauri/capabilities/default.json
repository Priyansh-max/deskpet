{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window (IPC + events).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
