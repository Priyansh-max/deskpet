{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the chip and settings windows (IPC + events).",
  "windows": ["main", "settings"],
  "permissions": ["core:default", "core:window:allow-close", "core:app:allow-version"]
}
